name: contracts
description: Code generator to use smart contracts with a Dart API.
# version: 0.1.0
# homepage: https://www.example.com
# author: Simon Binder <email@example.com>

environment:
  sdk: '>=2.1.0 <3.0.0'

dependencies:
  web3dart: ">=1.0.0-rc.0 <2.0.0"
  build: ^1.1.4
  path: ^1.6.2

dev_dependencies:
  pedantic: ^1.0.0
  build_runner: ^1.3.0
  test: ^1.0.0
