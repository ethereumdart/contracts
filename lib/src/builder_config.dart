class BuilderConfig {

  // extracting them into config because we might want to introduce a
  // "javascript" mode in the future. For JS, these values would have to be
  // reduced.

  final int dartIntSize = 64;
  final int dartUintSize = 63;

}